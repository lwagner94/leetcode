import math

# class Solution:
#     def isPalindrome(self, x: int) -> bool:
#         if x < 0: 
#             return False
#         digits = math.floor(math.log10(100)) + 1
        
#         first_half = x // (10 ** (math.ceil(digits / 2)))
#         second_half = x % (10 ** (math.floor(digits / 2)))
        
#         print(first_half, second_half)
        
#         for in in range(math.floor(digits / 2)):
            
            